#include "amigos.h"

int main() {
  banner();         // Shows the Banner
  authentication(); // Gives Access to the User Database
  initialize();     // Initializes the Data Structures
  mainMenu();       // Renders the Graphical User Interface
}